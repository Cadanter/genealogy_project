[project]
name = "genealogy-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.6",
    "mysql>=0.0.3",
    "pillow>=12.2.0",
    "python-decouple>=3.8",
]
